import { BearCounter, Controls } from "./StoreTest";
import TestButton from "./TestButton";

export default function TestComponent() {
  return (
    <div>
      <BearCounter />
      <Controls />
      <TestButton />
    </div>
  );
}
