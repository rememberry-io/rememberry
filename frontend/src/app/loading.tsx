import FlowBackground from "@/components/Flow/FlowBackground/flowBackground";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <FlowBackground />;
}
